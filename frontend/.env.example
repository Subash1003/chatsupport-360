# Vite only exposes variables that start with VITE_ to the browser.
# That is a safety feature: anything in this file ends up in the JavaScript
# bundle and is readable by any visitor. NEVER put an API key here.
VITE_API_BASE_URL=http://localhost:5000/api
